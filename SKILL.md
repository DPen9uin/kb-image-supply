---
name: kb-image-supply
description: 知识库配图补给。AstrBot 执行知识库检索（astr_kb_search）后触发：结合检索结果与对话语义主动判断是否需要配图；需要时，在本地 /AstrBot/data/kb_assets/ 下查找与知识库文档同名的源文件（pdf/pptx/docx 等），定位检索内容在文件中的位置并提取对应图片发送给用户。当用户要求出题、讲解图文内容、看示意图/显微照片/结构图/图表，或检索结果含图注文字（如"显微照片""示意图""图2-1"）时使用。
---

<!-- by DPenguin -->

# 知识库配图补给

在知识库检索返回之后，判断本次内容是否需要图片辅助，需要则从本地同名源文件中提取对应图片发给用户。

## 目录约定（与知识库文档同名）

- 源文件根目录：`/AstrBot/data/kb_assets/`
- 第一层目录名 = **知识库名**。例：知识库名「高中生物」→ `/AstrBot/data/kb_assets/高中生物/`
- 第二层 = **与知识库文档同名的源文件**（pdf / pptx / docx 均可）。例：知识库文档「高中生物 必修2.pdf」→ `/AstrBot/data/kb_assets/高中生物/高中生物 必修2.pdf`
- 与知识库文档的对应关系：知识库文档名 → kb_assets 下同名文件
- 若 kb_assets 下没有对应同名**目录**（即知识库名那层）：明确提醒用户目录缺失，给出应创建的路径

## 工作流

### 0. 静默执行原则

- **找图过程不向用户报告**：从定位源文件、搜索关键词、渲染/提取图片到保存临时文件，整个过程都是内部操作，**不要**在对话里播报「我先搜索定位一下」「正在提取图片」「图片已就位」这类过程性说明
- **结果中直接附图**：图片提取完成后，直接在当轮回复的结果中附上图片（用 `send_message_to_user` 发送），配图说明并入回复正文（如「这是教材第45页 图3-5，……」，可作为对图片内容的介绍，不要写成操作报告）
- 仅当**失败**需要用户配合时（源文件缺失、定位不到图、图片与内容不符）才向用户说明情况

### 1. 判断是否需要图片

满足以下任一条件即判定需要图：

- 用户明确要求：看图 / 配图 / 示意图 / 图片 / 照片 / 图几 / 结构图 / 图表
- 检索结果的文本里出现图注特征，如「照片」「示意图」「模式图」「结构图」「图X-X」「放大倍数」「电镜照片」「如图」
- 用户任务是出题、识图、看图填空、比较图像类，且题目通常依赖图形表达（遗传系谱图、细胞结构、器官图等）
- 内容本质需要视觉信息才能完整作答（描述形态、读图分析）

不满足则不需要图片，正常文字回答即可。

### 2. 确定知识库名与文档名，定位同名源文件

- 从 `astr_kb_search` 检索结果中提取知识库名与来源文档名（检索返回通常带来源标识）
- 源文件 = `./kb_assets/<知识库名>/<来源文档名>`
- 文档名不明确时，依据对话上下文推断
- 源文件不存在时：
  - 目录不存在 → 提醒用户：`./kb_assets/<知识库名>/` 不存在，请按约定放置
  - 文件不存在 → 提醒用户：同名文件缺失，列出该目录现有文件供确认

### 3. 在源文件中定位检索内容对应的图片

- **PDF**：用 PyMuPDF（`import pymupdf`）打开；从检索结果提取定位线索，**全文档搜索关键词**（如"噬菌体侵染"）定位页面，不要直接按检索结果给的教材页码取页——教材页码与 PDF 页面索引通常不对应；列出该页图片，按尺寸/位置筛选正文插图（排除水印/装饰小图），选与检索内容图注最匹配的一张
- **PPTX**：用 python-pptx 打开；按 slide 文本搜索关键词，提取对应 slide 中的图片（`shape.shape_type == PICTURE` 或图片填充）
- **DOCX**：用 python-docx 打开；按段落/表格文本匹配关键词，提取对应内嵌图片
- 关键词优先级：用户原话 → 检索结果图注/内容关键词 → 专有名词/图号

### 4. 提取为临时文件并发送

- 提取出的图片保存到**临时目录**（如 `/tmp/kb_img/` 或系统临时目录），**绝不写入 `./kb_assets/`**
- 当前会话：用 `send_message_to_user`，`type: "image"`，`path` 填临时图片路径；附一句说明（如"这是教材第45页 图3-5，噬菌体侵染细菌的电镜照片"）
- 用户指定了其他群/用户：用 `send_to_group_tool` / `send_to_private_user`
- 同一内容有多张候选图时，按与检索内容的相关度取最贴合的一张发送，其余不发送，除非用户要求全部

### 5. 发送后清理临时文件

- 发送完成后**立即删除**本次提取的临时图片文件（用 `file_remove`），保持 kb_assets 与临时目录纯净，不残留提取产物

## 兜底与失败处理

- **提取出的图片与检索内容明显不符**（如页码对不上、内容无关）：不发送，向用户说明文件名/页码与实际内容的差异，请用户确认源文件是否为知识库对应的完整文件
- **定位不到关键词**：尝试相邻页/章节；仍找不到则告知用户"文件中未检索到相关内容"，不猜测发送
- **目录/文件缺失**：明确提醒用户按约定补放，不静默跳过
- **检索结果不含任何图注线索且用户未要求图**：按第 1 条判定，不触发配图
- 始终优先保证：不发送错误的图，比漏发一张图更严重